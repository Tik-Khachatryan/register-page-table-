let inputs           = document.querySelectorAll('input');let btn              = document.querySelector('button');let lastDiv          = document.getElementById('table');let lables           = document.querySelectorAll('label');let p                = document.querySelectorAll('p');let regexForMail     = new RegExp('^[a-zA-Z0-9.!#$%&\'*+/=?^_`{|}~-]+@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)*$');let letters          = /^[A-Za-z]+$/;let regexForPassword = new RegExp("^(((?=.*[a-z])(?=.*[A-Z]))|((?=.*[a-z])(?=.*[0-9]))|((?=.*[A-Z])(?=.*[0-9])))(?=.{6,})");const createTable = (data, row, err) => {    let x = true;    let table   = document.createElement("table");    let thead   = document.createElement("thead");    let tbody   = document.createElement("tbody");    let headRow = document.createElement("tr");    let tr      = document.createElement("tr");    row.forEach((item) => {        let th = document.createElement("th");        th.append(item.textContent);        headRow.append(th);    });    thead.append(headRow);    table.append(thead);    data.forEach((item) => {        switch (item.name) {            case 'email':                if (!regexForMail.test(item.value)) {                    item.nextElementSibling.innerHTML = "Incorrect email";                }                break;            case 'name':                if (!item.value.match(letters)) {                    item.nextElementSibling.innerHTML = "Name must be only letters";                }                break;            case 'lastName':                if (!item.value.match(letters)) {                    item.nextElementSibling.innerHTML = "Last name must be only letters";                }                break;            case 'password':                if (!regexForPassword.test(item.value)) {                    item.nextElementSibling.innerHTML = "Password must be letters and numbers";                }                break;            default:                console.log("default");                break        }        if (item.value.length !== 0) {            let td = document.createElement("td");            td.append(item.value);            tr.append(td);            tbody.append(tr);            item.value = '';        }    });    for (let i = 0; i < err.length; i++) {        if (err[i].innerText !== '') {            x = false;            break        }    }    if (x === true) {        table.append(tbody);        table.setAttribute('border', '2');        return table;    } else {        return "Error validation"    }};btn.onclick = () => {    lastDiv.append(createTable(inputs, lables, p));};